import React from 'react';

const Off = () => {
  const containerStyle = {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    height: '100vh',
  };

  return (
    <div style={containerStyle}>
     <h1>OFFF</h1>
    </div>
  );
}

export default Off;
